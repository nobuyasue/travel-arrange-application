# Travel Arrange Application — 施工プラン（実装計画）

## 1. アプリ概要

**コンセプト：** 旅行プランを作るAIではなく、旅行プランを予約できる状態まで持っていくAI

ユーザーが旅行条件を入力すると、AIが旅程を生成し、予約が必要な項目を自動抽出。
各予約サイトへの条件付きリンクを生成し、予約直前まで最短で誘導する。

---

## 2. ターゲットユーザー（MVP）

- 20代後半〜40代の個人旅行者
- 海外・国内旅行を年に数回する
- 旅行計画は好きだが予約作業が面倒
- Expedia、Booking、Klook等を毎回行き来している

---

## 3. MVP機能一覧

| # | 機能 | 内容 | 優先度 |
|---|------|------|--------|
| 1 | 条件入力 | 行き先・日程・予算・同行者・旅行スタイルを入力 | 必須 |
| 2 | AI旅程生成 | 入力条件をもとに日別の旅行プランを自動生成 | 必須 |
| 3 | 予約タスク抽出 | 旅程から予約が必要な項目（航空券・ホテル・アクティビティ）を自動抽出 | 必須 |
| 4 | 予約候補比較 | 各項目の候補サイト・価格・評価・キャンセル条件を比較表示 | 必須 |
| 5 | 予約サイト遷移 | 条件（日付・人数等）を保持した状態で予約サイトへ誘導 | 必須 |
| 6 | 予約進捗管理 | 予約済み/未予約/保留のステータス管理 | 必須 |

---

## 4. 画面構成

```
[ホーム] → [条件入力] → [旅程表示] → [予約タスク一覧] → [候補比較] → [予約サイトへ遷移]
                                            ↓
                                      [予約進捗管理]
```

### 4.1 各画面の詳細

| 画面 | 主な要素 |
|------|----------|
| ホーム | 新規旅行作成ボタン、進行中の旅行一覧 |
| 条件入力 | 日程、行き先、予算、同行者数、旅行スタイル、優先事項 |
| 旅程表示 | 日ごとのタイムライン、編集機能、代替案表示 |
| 予約タスク一覧 | 予約必要項目、ステータス、優先度 |
| 候補比較 | 価格、評価、立地、キャンセル条件、公式/OTA区分 |
| 予約実行 | 条件確認、予約サイトへのリンク、完了チェック |

---

## 5. 技術スタック（案）

| レイヤー | 技術 | 備考 |
|----------|------|------|
| フロントエンド | Next.js (React) + TypeScript | Web版。将来的にモバイル対応 |
| UIフレームワーク | Tailwind CSS + shadcn/ui | 高速開発向き |
| バックエンド | Next.js API Routes or FastAPI | API層 |
| AI | Claude API（Anthropic） | 旅程生成・予約タスク抽出 |
| データベース | Supabase (PostgreSQL) | 認証・DB・ストレージ一体型 |
| 認証 | Supabase Auth | Google/メールログイン |
| 外部API | Skyscanner / Booking / Klook 等 | 航空券・ホテル・アクティビティ |
| デプロイ | Vercel | Next.jsとの相性が良い |

---

## 6. データ設計

### trips（旅行）
| カラム | 型 | 説明 |
|--------|-----|------|
| trip_id | UUID | 主キー |
| user_id | UUID | ユーザーID |
| destination | TEXT | 目的地 |
| start_date | DATE | 開始日 |
| end_date | DATE | 終了日 |
| budget | INT | 予算（円） |
| travelers | INT | 人数 |
| preferences | JSONB | 嗜好（スタイル、優先事項等） |
| created_at | TIMESTAMP | 作成日時 |

### itinerary_days（日程）
| カラム | 型 | 説明 |
|--------|-----|------|
| day_id | UUID | 主キー |
| trip_id | UUID | FK: trips |
| day_number | INT | 何日目 |
| activities | JSONB | 訪問場所・時刻・移動手段 |

### booking_tasks（予約タスク）
| カラム | 型 | 説明 |
|--------|-----|------|
| task_id | UUID | 主キー |
| trip_id | UUID | FK: trips |
| task_type | TEXT | flight / hotel / activity |
| task_name | TEXT | 表示名 |
| date | DATE | 対象日 |
| status | TEXT | pending / booked / on_hold |
| selected_option_id | UUID | 選択した候補 |

### booking_options（予約候補）
| カラム | 型 | 説明 |
|--------|-----|------|
| option_id | UUID | 主キー |
| task_id | UUID | FK: booking_tasks |
| provider | TEXT | 予約サイト名 |
| title | TEXT | プラン名 |
| price | INT | 価格 |
| currency | TEXT | 通貨 |
| rating | FLOAT | 評価 |
| cancellation_policy | TEXT | キャンセル条件 |
| deeplink_url | TEXT | 条件付きURL |
| deeplink_params | JSONB | 遷移時パラメータ |

---

## 7. 開発フェーズ

### Phase 1: 基盤構築（1〜2週間）
- [ ] プロジェクト初期化（Next.js + TypeScript + Tailwind）
- [ ] Supabase セットアップ（DB・認証）
- [ ] テーブル作成（マイグレーション）
- [ ] 基本レイアウト・ルーティング
- [ ] ユーザー認証（ログイン/サインアップ）

### Phase 2: 旅程生成（2〜3週間）
- [ ] 条件入力画面の実装
- [ ] Claude API 連携（旅程生成プロンプト設計）
- [ ] 旅程表示画面（日別タイムライン）
- [ ] 旅程の編集・再生成機能

### Phase 3: 予約タスク管理（2〜3週間）
- [ ] AIによる予約タスク自動抽出ロジック
- [ ] 予約タスク一覧画面
- [ ] ステータス管理（未予約/予約済み/保留）
- [ ] 優先順位の自動提案

### Phase 4: 予約候補・比較（2〜3週間）
- [ ] 外部API連携（航空券・ホテル・アクティビティ）
- [ ] 候補比較画面の実装
- [ ] 価格・評価・キャンセル条件の表示
- [ ] ディープリンク生成ロジック

### Phase 5: 予約導線・仕上げ（1〜2週間）
- [ ] 条件付き予約サイト遷移の実装
- [ ] 予約完了チェック機能
- [ ] UI/UXの調整・レスポンシブ対応
- [ ] テスト・バグ修正

---

## 8. MVP対象外（将来の拡張）

- 価格アラート（価格変動の通知）
- 複数人の共同編集
- 旅程変更時の再予約提案
- 雨天時の代替プラン
- 現地移動の自動最適化
- 旅行中の当日アシスタント
- 領収書・予約番号の一元管理
- モバイルアプリ（React Native / Flutter）

---

## 9. リスクと対策

| リスク | 対策 |
|--------|------|
| 予約サイトごとに仕様が異なる | MVP時は主要サイト3〜5個に限定 |
| ディープリンクが作れないサイトがある | リンク生成できない場合はサイトトップ+手順案内で代替 |
| 価格・在庫がリアルタイムで変動 | キャッシュ期間を短く設定、「参考価格」と明示 |
| 利用規約・Bot対策 | 自動操作は行わず、ユーザー操作の補助に徹する |
| AI生成の旅程精度 | プロンプトの継続改善、ユーザーフィードバック反映 |

---

## 10. 収益モデル（案）

1. **予約送客アフィリエイト** — 予約サイトへの送客で成果報酬
2. **月額課金（Pro）** — 高度な旅程最適化、無制限の比較、共同編集
3. **B2B提供** — 旅行代理店・出張手配・富裕層向けコンシェルジュ
